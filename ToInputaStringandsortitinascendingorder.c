#include<stdio.h>
int main()
{char a[90];
int i=1,min;
min=a[0];
printf("Enter the String\n");
fgets(a,80,stdin);
while(a[i]!='\0')
{
    if(a[i]>=65 && a[i]<=90)
    {
        
    }
}
}